<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="user-mapper">

	<!-- <select id = "emailDuplicateCheck" parameterType = "String" resultType = "Integer">
		SELECT COUNT(*)
		FROM USER
		WHERE EMAIL = #{email}
	</select>
	
	<insert id = "insertUser" parameterType="UserEntity" resultType = "Integer">
		INSERT INTO USER(EMAIL, PW, EMPLYEE_ID, IS_ADMIN, IS_REPORT_ACTIVATE, REPORT_TIME, PHONE_NUMBER, NAME)
		VALUES(#{email}, #{}, #{}, #{}, #{}, #{}, #{}, #{}, #{}, #{},)
	</insert> -->

	<!-- <insert id="insertClient">
		INSERT INTO CLIENT(USER_ID, USER_PW, USER_NAME, USER_PHONE, USER_ADDR, USER_ROLE) 
		VALUES(#{user_id},#{user_pw},#{user_name},#{user_phone},#{user_addr},"USER")
	</insert>
	
	<update id="editClientByManager">
		UPDATE CLIENT
		SET USER_NAME = #{user_name}, USER_ADDR = #{user_addr}, USER_PHONE = #{user_phone}, USER_ROLE = #{user_role}
		WHERE USER_ID = #{user_id}
	</update>
	
	<delete id="deleteClient">
		DELETE
		FROM CLIENT
		WHERE USER_ID = #{user_id} 
	</delete> -->
</mapper>